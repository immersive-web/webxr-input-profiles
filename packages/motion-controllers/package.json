{
  "name": "@webxr-input-profiles/motion-controllers",
  "version": "0.2.0",
  "description": "",
  "main": "dist/motion-controllers.module.js",
  "files": [
    "package.json",
    "LICENSE",
    "README.md",
    "dist/motion-controllers.module.js",
    "src/**"
  ],
  "scripts": {
    "clean": "node -e \"try { require('fs').rmdirSync('./dist', { recursive: true }); } catch {}\"",
    "build": "rollup -c",
    "cleanBuild": "npm run clean && npm run build",
    "watch": "rollup -c -w -m inline",
    "test": "echo \"Run tests from root\" && exit 1"
  },
  "devDependencies": {
    "@webxr-input-profiles/assets": "^0.2.0"
  }
}
